<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene. layout.*?>

<?import javafx.scene.layout.BorderPane?>
<BorderPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.passman.desktop.ui.dashboard.DashboardController"
            stylesheets="@../styles/main.css">

    <!-- Top Bar -->
    <top>
        <VBox styleClass="top-bar">
            <HBox alignment="CENTER_LEFT" spacing="10" styleClass="header">
                <padding>
                    <Insets top="15" right="20" bottom="15" left="20"/>
                </padding>

                <Label text="🔐 PassMan" styleClass="app-title"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Label fx:id="welcomeLabel" text="Welcome!" styleClass="welcome-label"/>
                <Button text="Logout" onAction="#handleLogout" styleClass="logout-button"/>
            </HBox>

            <!-- Search and Actions Bar -->
            <HBox alignment="CENTER_LEFT" spacing="10" styleClass="toolbar">
                <padding>
                    <Insets top="10" right="20" bottom="10" left="20"/>
                </padding>

                <TextField fx:id="searchField" promptText="🔍 Search passwords..."
                           prefWidth="300" styleClass="search-field"/>

                <Region HBox.hgrow="ALWAYS"/>

                <Button fx:id="addButton" text="➕ Add" onAction="#handleAddCredential" styleClass="action-button"/>
                <Button text="🎲 Generate" onAction="#handleGeneratePassword" styleClass="action-button"/>
                <Button text="💾 Backup" onAction="#handleBackup" styleClass="action-button"/>
            </HBox>
        </VBox>
    </top>

    <!-- Main Content -->
    <center>
        <BorderPane>
            <!-- Sidebar -->
            <left>
                <VBox spacing="5" styleClass="sidebar" prefWidth="200">
                    <padding>
                        <Insets top="10" right="10" bottom="10" left="10"/>
                    </padding>

                    <Label text="CATEGORIES" styleClass="sidebar-header"/>
                    <Button text="🏠 All Passwords" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="⭐ Favorites" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="🔒 Passwords" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="📝 Secure Notes" onAction="#handleNotes" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="🆔 Identity Cards" onAction="#handleIdentityCards" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="📁 File Vaults" styleClass="sidebar-button" maxWidth="Infinity"/>

                    <Separator/>

                    <Label text="FEATURES" styleClass="sidebar-header"/>
                    <Button text="📊 Analytics" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="📈 Graph View" onAction="#handleGraph" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="🎮 Quiz" onAction="#handleQuiz" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="📱 QR Share" onAction="#handleQRShare" styleClass="sidebar-button" maxWidth="Infinity"/>
                    <Button text="⚙️ Admin" onAction="#handleAdmin" styleClass="sidebar-button" maxWidth="Infinity"/>
                </VBox>
            </left>

            <!-- Password Table -->
            <center>
                <VBox spacing="10">
                    <padding>
                        <Insets top="20" right="20" bottom="20" left="20"/>
                    </padding>

                    <Label text="Your Passwords" styleClass="section-title"/>

                    <TableView fx:id="credentialsTable" VBox.vgrow="ALWAYS">
                        <columns>
                            <TableColumn text="Title" prefWidth="200"/>
                            <TableColumn text="Username" prefWidth="150"/>
                            <TableColumn text="URL" prefWidth="200"/>
                            <TableColumn text="Age" prefWidth="100"/>
                            <TableColumn text="Strength" prefWidth="100"/>
                        </columns>
                    </TableView>
                </VBox>
            </center>
        </BorderPane>
    </center>

</BorderPane>