<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <!-- Biometric feature -->
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".PassManApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PassMan.Splash"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- Splash Screen Activity -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PassMan.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activity -->
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize" />

        <!-- Credential Detail Activity -->
        <activity
            android:name=".ui.credential.CredentialDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Add/Edit Credential Activity -->
        <activity
            android:name=".ui.credential.AddEditCredentialActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Password Generator Activity -->
        <activity
            android:name=".ui.generator.PasswordGeneratorActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- QR Scanner Activity -->
        <activity
            android:name=".ui.qr.QRScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan.FullScreen" />

        <!-- File Vault Activity -->
        <activity
            android:name=".ui.vault.FileVaultActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Vault Details Activity -->
        <activity
            android:name=".ui.vault.VaultDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:parentActivityName=".ui.vault.FileVaultActivity" />

        <!-- Secure Notes Activity -->
        <activity
            android:name=".ui.notes.SecureNotesActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Secure Note Detail Activity -->
        <activity
            android:name=".ui.notes.SecureNoteDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.notes.SecureNotesActivity" />

        <!-- Cards Activity -->
        <activity
            android:name=".ui.cards.CardsActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Card Detail Activity -->
        <activity
            android:name=".ui.cards.CardDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.PassMan"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.cards.CardsActivity" />

        <!-- FileProvider for sharing QR codes and files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Auto-lock receiver -->
        <receiver
            android:name=".receiver.ScreenLockReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver to restart notification scheduler -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
